import 'package:flutter/material.dart';

class ProductsFavoritePage extends StatelessWidget {
  const ProductsFavoritePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
